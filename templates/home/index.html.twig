{% extends 'base.html.twig' %}
{% block title %}Accueil{% endblock %}
{% block body %}
<div class="index__banner">
    <h1>Blablabla</h1>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Non illo aliquid accusantium dolorem ex! Beatae numquam, sint dolor officiis quod, obcaecati porro, voluptas quam aperiam a vitae vero? Esse, quibusdam!</p>
</div>
<div class="index__second-section">
    <h1><i class="fas fa-film"></i> Des films à succès</h1>
    <div id="carousel" class="second-section__popular-movies">
        {% for movie in popularMovies %}
        <a href="movie/{{ movie.id }}" class="popular-movies__movie">
            <img src="{{ movie.ApiResponse.image }}" alt="Affiche du film">
            <div class="movie__info">
                <h3>{{ movie.ApiResponse.title }} <span class="movie__year">{{ movie.ApiResponse.year }}</span></h3>
                <i class="fas fa-star"></i>
                <span>{{ movie.ApiResponse.imDbRating }}</span>
                <p>{{ movie.ApiResponse.plotLocal|length > 150 ? movie.ApiResponse.plotLocal|slice(0, 150) ~ '...' : movie.ApiResponse.plotLocal }}</p>
            </div>
        </a>
        {% endfor %}
    </div>
</div>
{% endblock %}
