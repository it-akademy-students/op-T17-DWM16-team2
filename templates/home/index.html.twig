{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

<div class="index__banner">
    <h1>Bienvenue sur Movie Home</h1>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Non illo aliquid accusantium dolorem ex! Beatae numquam, sint dolor officiis quod, obcaecati porro, voluptas quam aperiam a vitae vero? Esse, quibusdam!</p>
</div>
<div class="index__second-section">
    <h1><i class="fas fa-film primary-color"></i> Des films à succès</h1>
    <div class="second-section__popular-movies">
        {% for movie in popularMovies %}
            <div class="popular-movies__movie card">
                <a href="{{ path('movie', {'id': movie.db.id}) }}">
                    <img class="poster-movie-card" src="{{ movie.0.Poster }}" alt="Affiche du film">
                </a>
                <div class="movie__info">
                    <h3>{{ movie.0.Title }} <span class="movie-year primary-color">{{ movie.0.Year }}</span></h3>
                    <i class="fas fa-star primary-color"></i>
                    <span>{{ movie.0.imdbRating }}</span>
                    <p>{{ movie.0.Plot|length > 150 ? movie.0.Plot|slice(0, 150) ~ '...' : movie.0.Plot }}</p>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
