{% extends 'base.html.twig' %}

{% block title %}{{ movie.title }}{% endblock %}

{% block body %}
<div class="wrapper">
    <h1>{{ movie.title }}</h1>
    <span>{{ movie.year }}</span>
</div>
{% endblock %}
